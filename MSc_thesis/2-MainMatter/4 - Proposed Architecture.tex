%!TEX root = ../template.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% 4 - Proposed Architecture.tex
%% NOVA thesis document file
%%
%% Chapter with proposed architecture
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% chktex-file 24
\typeout{NT FILE 4 - Proposed Architecture}%

\chapter{Implementation}
\label{cha:proposed-architecture}

This chapter documents how the testbed was actually built: the physical hardware, the network topology connecting it, and the software stack for both the Core and the two Radio Access Networks. Section~\ref{sec:system-overview} gives an overview of the overall system and how its four machines are laid out and connected. Section~\ref{sec:hardware-components} details the physical hardware assets, software-defined radios, clock synchronization equipment, and user equipment. Section~\ref{sec:core} describes the implementation of the Core Network, Section~\ref{sec:ran} outlines the structure of the Radio Access Network for both generations, and Section~\ref{sec:ue} describes the user equipment used to connect to the network.
%CHANGED BY CLAUDE
A step-by-step guide to building both networks is provided in Appendix~\ref{app:4G Network Setup}.



\section{System Architecture Overview}
\label{sec:system-overview}

%CHANGED BY CLAUDE
The testbed is built around four machines rather than a single pair: one running the 5G \gls{RAN}, another running the 4G \gls{RAN}, a third running the Core network (shared by both generations), and a fourth reserved for a 5G \gls{NSA} deployment that was ultimately not implemented. Each working \gls{RAN} is kept on its own dedicated machine rather than sharing a single host between radio access technologies, so that the 4G and 5G networks can run independently of one another rather than being intermingled on the same machine. This choice follows directly from one of the goals of this dissertation: to leave a working network testbed in place, rather than a single disposable experiment, for future projects to build on and explore.

All 4 machines are Dell Optiplex Micro 7080 machines, with the specifications listed in Table~\ref{tab:hardware-specifications}. The machines are connected to each other through an ethernet switch, with static IP addresses assigned to each one, as shown in Figure~\ref{fig:ip-addresses}.

\begin{figure}
    \centering
    \includegraphics[width=\textwidth]{IP-Addresses.pdf}
    \caption{Network topology of the four machines, showing the role of each machine and its static IP address. The machine in gray was not used in the actual testbed, but was reserved for a 5G \gls{NSA} deployment.}
    \label{fig:ip-addresses}
\end{figure}

A view of the laboratory bench, showing all four machines, is shown in Figure~\ref{fig:bench-machines}.

\begin{figure} [htbp]
    \centering
    \includegraphics[width=\textwidth]{machines.jpg}
    \caption{The bench, showing all 4 machines. From left to right: 4G RAN, 5G NSA (unused), Core and 5G RAN. Connected to the 4G RAN machine is the bladeRF xA4, and on the rightmost side, connected to the 5G RAN machine, is the NI B2901.}
    \label{fig:bench-machines}
\end{figure}

\section{Hardware Components}
\label{sec:hardware-components}
The machines chosen for this project are Dell Optiplex Micro 7080 machines, with the following specifications:
\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|c|}
        \hline
        \textbf{Component} & \textbf{Machine 1} & \textbf{Machine 2} \\
        \hline
        CPU & Intel Core i7-14700T & Intel Core i7-14700T \\
        \hline
        RAM & 16 GB DDR5 & 16 GB DDR5 \\
        \hline
        Operating System & Ubuntu 22.04 LTS & Ubuntu 22.04 LTS \\
        \hline
        Storage & 512 GB SSD & 512 GB SSD \\
        \hline
    \end{tabular}
    \caption{Specifications of the machines used for the 5G network setup.}
    \label{tab:hardware-specifications}
\end{table}

For the radio, two \glspl{SDR} were chosen:
\begin{itemize}
    \item \underline {National Instruments USRP B210}: This is a mid-level \gls{SDR} that uses UHD, the gold standard for \gls{SDR} development. These boards are widely used in research projects and testbeds, and they are natively compatible with most \gls{SDR} software.
    \item \underline {Nuand bladeRF xA4}: This is a cheaper mid-level \gls{SDR} that uses a proprietary library, libbladeRF, for communication with the host machine. It is less used than the USRP B210 in research projects, as it is not directly compatible with most software. Through the use of a wrapper library, it is possible to use this \gls{SDR} with most software which don't natively support it.
\end{itemize}

Both of these boards use an Analog Devices AD9361 transceiver, which makes their \glsxtrshort{RF} capabilities theoretically similar. The main difference between them are their \glspl{FPGA}, in which the B210 uses a Xilinx Spartan-6, while the bladeRF xA4 uses an Intel Altera Cyclone V.

Along with the \glspl{SDR}, a Leo Bodnar \gls{GPSDO} was chosen to provide a stable clock source for the radios, which is crucial for the proper operation of the 5G network, given the strict timing requirements of the 5G standard, and TDD operation. The \gls{GPSDO} provides a stable 10 MHz reference clock and a 1PPS signal, which are used to synchronize the \glspl{SDR} and ensure accurate timing for the transmission and reception of signals.

Connected to the 4G \gls{RAN} machine is the Nuand bladeRF xA4 (Figure~\ref{fig:bladerf}), which was used for the 4G deployment. This board, unlike the B2901, was powered entirely over its USB connection to the RAN machine, with no separate power supply used.

Connected to the 5G \gls{RAN} machine is a NI B2901 (Figure~\ref{fig:b2901-gpsdo}), the National Instruments designation for the USRP B210 described above. In addition to its USB connection to the RAN machine, the B2901 was connected to a wall socket through its own power supply, which was required to sustain the board at the gain settings used during testing. Along with the board, the Leo Bodnar GPSDO was connected to provide both a stable clock source and a 1PPS signal for synchronization. The GPSDO's GPS lock was verified using \texttt{lbgpsdo}~\cite{lbgpsdo}, an open-source command-line utility for configuring and querying Leo Bodnar GPSDO devices.

\begin{figure} [htbp]
    \centering
    \includegraphics[width=\textwidth]{bladeRF.jpg}
    \caption{The Nuand bladeRF xA4, connected and cabled on the bench.}
    \label{fig:bladerf}
\end{figure}

\begin{figure} [htbp]
    \centering
    \includegraphics[width=\textwidth]{b2901_gpsdo_collage.jpg}
    \caption{The B2901 and the Leo Bodnar GPSDO, as connected on the bench.}
    \label{fig:b2901-gpsdo}
\end{figure}

For both radios, the antennas used were the same. The 4G rig used a BT-200 \gls{LNA} on the RX port and a BT-100 power amplifier on the TX port, both Nuand components, seen in Figure~\ref{fig:biastee-components}. The 5G rig had no additional RF front-end components.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{biastee components.jpg}
    \caption{The BT-200 \gls{LNA} and BT-100 power amplifier bias tees, mounted on the bladeRF xA4's RX1 and TX1 ports respectively.}
    \label{fig:biastee-components}
\end{figure}

\section{Core}
\label{sec:core}

The core network is implemented using Open5GS~\cite{open5gs_repo}, an open-source project that provides both a complete 5G core network (5GC) and a 4G \gls{EPC} implementation, following the \gls{3GPP} Release 17 specifications. This dual support facilitates the 4G/5G comparison at the centre of this dissertation possible on shared hardware, as the same Open5GS deployment serves as the core for both generations, so the 4G baseline and the 5G network under test differ only in their \gls{RAN}. Open5GS is designed to be modular, with each network function individualized and implemented as a separate process, each with its own configuration file on the machine. Subscriber data for both generations is held in a single MongoDB database, which Open5GS uses as its backend. The core network is deployed on its own dedicated machine.

%CHANGED BY CLAUDE
The Open5GS~\cite{open5gs_repo} core ran at version 2.4.6. Harking back to the reusability objective stated in the Introduction, all configuration files for the core were made available in a GitHub repository~\cite{thesis_repo}, and a script to deploy them directly onto the core machine was created and made public, allowing anyone to redeploy the same core configuration without having to reconstruct it by hand. The script, \texttt{deploy\_open5gs\_configs.sh}, sparse-checks out the configuration files from the project's GitHub repository directly onto the core host. This approach ensures that the testbed's configuration can be easily replicated in the future, supporting the goal of reusability and facilitating further research or experimentation based on this work. Full step-by-step installation and configuration instructions for Open5GS are given in Appendix~\ref{app:4G Network Setup}.

\section{\gls{RAN}}
\label{sec:ran}

The \gls{RAN} is implemented using srsRAN~\cite{ocudu_project} (renamed OCUDU partway through this project; see Section~\ref{sec:srsran}), an open-source software suite that provides a complete 5G \gls{NR}, following the \gls{3GPP} Release 17 specifications. Written in both C and C++ languages, it is modular by design, allowing the developer to customize both the \gls{CU} and {DU} components of the gNodeB separately. Each generation's \gls{RAN} is deployed on its own dedicated machine, and connects to its \gls{SDR} via USB 3.0.

\subsection{5G RAN}
\label{sec:ran-5g}

%CHANGED BY CLAUDE
On the software side, the 5G srsRAN \gls{RAN}~\cite{ocudu_project} ran at commit 29dc1aede7, with the static configuration presented in Table~\ref{tab:ocudu-config}.

\begin{table}[htbp]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Parameter} & \textbf{Value} \\
        \hline
        Band & n78 (TDD)\\
        \hline
        Downlink/Uplink centre frequency & 3410 MHz \\
        \hline
        \texttt{dl\_arfcn} & 627340 \\
        \hline
        Channel Bandwidth & 20 MHz \\
        \hline
        Subcarrier Spacing & 30 kHz \\
        \hline
        PLMN & 00101 \\
        \hline
        TAC & 7 \\
        \hline
        PCI & 1 \\
        \hline
        \texttt{tx\_gain} & 89.75 \\
        \hline
    \end{tabular}
    \caption{Static configuration for the 5G srsRAN \gls{RAN}.}
    \label{tab:ocudu-config}
\end{table}

%CHANGED BY CLAUDE
The parameter \texttt{rx\_gain} and the TDD structure were changed between testing rounds, and are discussed further in Chapter~\ref{cha:implementation}, Section~\ref{sec:test-methodology}.

\subsection{4G RAN}
\label{sec:ran-4g}

%CHANGED BY CLAUDE
The 4G srsRAN \gls{RAN}~\cite{srsran_4g_repo} ran at commit 6bcbd9e5b, with no configuration changes across testing rounds. The configuration parameters are presented in Table~\ref{tab:srsran4g-config}. Full installation and configuration steps for the 4G RAN, including the BladeRF driver and SoapySDR setup, are documented in Appendix~\ref{app:4G Network Setup}.

\begin{table}[htbp]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Parameter} & \textbf{Value} \\
        \hline
        Band & 7 (FDD) \\
        \hline
        Downlink centre frequency & 2680.0 MHz \\
        \hline
        Uplink centre frequency & 2560.0 MHz \\
        \hline
        Channel Bandwidth & 10 MHz (50 PRB) \\
        \hline
        PCI & 1 \\
        \hline
        PLMN & 00101 \\
        \hline
        TAC & 7 \\
        \hline
        Cell ID & 1 \\
        \hline
        \texttt{tx\_gain} & 66 \\
        \hline
        \texttt{rx\_gain} & 60 \\
        \hline
    \end{tabular}
    \caption{Static configuration for the 4G srsRAN \gls{RAN}. No parameters changed across testing rounds.}
    \label{tab:srsran4g-config}
\end{table}

\section{\glspl{UE}}
\label{sec:ue}

%CHANGED BY CLAUDE
The proposed \gls{UE} for the testbed was a Motorola Moto G56 5G, a mid-range smartphone chosen for its 5G and eSIM support. This did not end up matching what was actually used: the Moto G56 struggled to connect to the network during testing, for a cause that was never identified and no speculation is made here. A Samsung Galaxy Z Flip 5 (personal device) was brought in instead, and became the primary \gls{UE} for the actual test campaigns, running the CellularLab app to generate \texttt{iperf3} traffic. Both devices connect to the network using an eSIM profile, provisioned through a remote SIM provisioning service. Table~\ref{tab:ue-comparison} compares the two candidate devices; the eSIM provisioning process used to connect them to the network is documented in full in Appendix~\ref{ch:provisioning_esim}.

\begin{table}[htbp]
    \centering
    \begin{tabular}{|c|c|c|}
        \hline
        \textbf{Device} & \textbf{Chipset} & \textbf{RAM / Storage}\\
        \hline
        Motorola Moto G56 5G~\cite{gsmarena_moto_g56} & MediaTek Dimensity 7060 & 8\,GB / 512\,GB\\
        \hline
        Samsung Galaxy Z Flip5~\cite{gsmarena_z_flip5} & Qualcomm Snapdragon 8 Gen 2 & 12\,GB / 512\,GB \\
        \hline
    \end{tabular}
    \caption{Comparison of the two candidate \glspl{UE}. Both support \gls{NR} band n78, the band used by this testbed.}
    \label{tab:ue-comparison}
\end{table}
